The path can be modelled as a chain of segments -- the uplink, then the peer --
instead of a single endpoint pair, so flows to different places pool what they
share and keep separate what they do not. Enabled by the `dc-long-haul`
profile; the access-link profile is unchanged.
