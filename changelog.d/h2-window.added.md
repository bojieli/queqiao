`pathmeasure` gains `h2serve` and `h2` modes, which measure what an HTTP/2
receive window costs on a long path by running the same upload against servers
that differ only in the window they advertise.
